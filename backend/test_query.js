const path = require('path'); const dbPath = path.join(process.env.APPDATA, 'bestbill-desktop', 'bestbill.db'); const { DatabaseSync } = require('node:sqlite'); const db = new DatabaseSync(dbPath, { open: true }); const stmt = db.prepare('SELECT b.id as bill_id, o.table_id, t.table_number, b.final_amount, b.created_at FROM bills b JOIN orders o ON b.order_id = o.id LEFT JOIN tables t ON o.table_id = t.id'); console.log(stmt.all());
